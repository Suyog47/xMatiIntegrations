// "use strict";

// const { Templates } = require("./template-enum");

// // To create a random string for userId
// function generateRandomId(length) {
//     const characters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
//     return Array.from({ length }, () => characters[Math.floor(Math.random() * characters.length)]).join('');
// }

// // Function to get the template file based on the user’s selected template
// function getTemplateFile(selectedTemplate) {
//     return Templates[selectedTemplate];
// }

// module.exports = {
//     generateRandomId,
//     getTemplateFile
// };
